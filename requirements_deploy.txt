# Lightweight deployment requirements optimized for small storage targets (e.g., Replit)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
PyJWT>=2.8.0
cryptography>=41.0.0
openai>=1.0.0
requests>=2.31.0
httpx>=0.24.0
structlog>=23.1.0
prometheus-client>=0.19.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-fastapi>=0.40b0

# Note: Heavy ML packages (torch, transformers, spacy, streamlit, cuda libraries) intentionally omitted.
# Install them separately in larger environments only when needed for intelligence features.
