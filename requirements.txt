# Jimini AI Policy Gateway - Production Dependencies
# Lightweight deployment-optimized requirements (< 100MB total)

# Core FastAPI & Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Configuration & Data
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Security & Auth
PyJWT>=2.8.0
cryptography>=41.0.0

# OpenAI LLM Integration (lightweight client)
openai>=1.0.0

# HTTP Client & API
requests>=2.31.0
httpx>=0.24.0
aiohttp>=3.8.0

# Observability & Monitoring
structlog>=23.1.0
prometheus-client>=0.19.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-fastapi>=0.40b0

# SIEM & Integration (lightweight)
# elasticsearch and splunk-sdk are optional, not included in base deployment

# Development & Testing (not installed in production)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# ruff>=0.1.0
email-validator
flask
flask-cors
numpy
pandas
plotly
psutil
pytest
websocket
